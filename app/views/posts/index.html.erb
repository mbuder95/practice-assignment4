<div>
  <% for post in @posts %>
    <div>
      <h4>
        <%# Display the name of the user that created the post: %>
        <%# 1. find user who created post %>
        <% user = User.find_by({ "id" => post["username"] }) %>
        <%# 2. handle any potential bad data where the post is missing a user %>
        <% if user != nil %>
          <%= user["username"] %>
        <% else %>
          anonymous
        <% end %>
      </h4>
      <img src="<%= post["image"] %>">
      <p><%= post["body"] %></p>
    </div>
  <% end %>
</div>